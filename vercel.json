{
  "buildCommand": "npm install && cd packages/shared && npm run build && cd ../web && npm run build",
  "outputDirectory": "packages/web/dist",
  "rewrites": [
    {
      "source": "/api/:path*",
      "destination": "https://blood-test-startup-tracker-production.up.railway.app/api/:path*"
    }
  ]
}
